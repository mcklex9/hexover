package org.hexagonexample.application.ports.outbound;

import org.hexagonexample.domain.AccountId;
import org.springframework.stereotype.Service;

@Service
public class SoftCheckVerificationServiceImpl implements SoftCheckVerificationService {
    @Override
    public void softCheckAccount(AccountId accountId) {

    }
}
